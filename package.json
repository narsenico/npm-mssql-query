{
  "name": "npm-mssql-query",
  "version": "1.0.0",
  "description": "exec query on msql with node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "narsenico",
  "license": "MIT",
  "dependencies": {
    "mssql": "^2.2.0"
  }
}
